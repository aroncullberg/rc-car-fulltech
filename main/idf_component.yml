## IDF Component Manager Manifest File
dependencies:
  ## Required IDF version
  idf:
    version: '>=4.1.0'
  # # Put list of dependencies here
  # # For components maintained by Espressif:
  # component: "~1.0.0"
  # # For 3rd party components:
  # username/component: ">=1.0.0,<2.0.0"
  # username2/component2:
  #   version: "~1.0.0"
  #   # For transient dependencies `public` flag can be set.
  #   # `public` flag doesn't have an effect dependencies of the `main` component.
  #   # All dependencies of `main` are public by default.
  #   public: true
  espressif/mavlink: ^1.0.0~1
  joltwallet/littlefs: ==1.19.2
  h2zero/esp-libtelnet:
    git: "https://github.com/h2zero/esp-libtelnet"
  andyfinity/TinyGPSPlus-ESPIDF:
    git: "https://github.com/andyfinity/TinyGPSPlus-ESPIDF"
  aroncullberg/icm20948:
    git: "https://github.com/aroncullberg/icm20948"
  aroncullberg/EscDriver:
    git: "https://github.com/aroncullberg/DShot-ESP32RMT"


